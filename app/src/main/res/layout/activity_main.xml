<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:fitsSystemWindows="true"
    tools:context="com.yunkahui.datacubeper.base.MainActivity">

    <com.yunkahui.datacubeper.common.view.CustomViewPager
        android:id="@+id/view_pager"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <View
        android:background="#ccc"
        android:layout_width="match_parent"
        android:layout_height="0.5dp" />

    <RadioGroup
        android:id="@+id/radio_group"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/white"
        android:orientation="horizontal">

        <RadioButton
            android:id="@+id/rb_home"
            android:text="首页"
            android:checked="true"
            android:drawableTop="@drawable/main_tab_home_selector"
            style="@style/MainRadioButtonStyle" />

        <RadioButton
            android:id="@+id/rb_bill"
            android:text="账单"
            android:drawableTop="@drawable/main_tab_bill_selector"
            style="@style/MainRadioButtonStyle" />

        <RadioButton
            android:id="@+id/rb_card_test"
            android:text="卡·测评"
            android:drawableTop="@drawable/main_tab_card_test_selector"
            style="@style/MainRadioButtonStyle" />

        <RadioButton
            android:id="@+id/rb_share"
            android:text="分享"
            android:drawableTop="@drawable/main_tab_share_selector"
            style="@style/MainRadioButtonStyle" />

        <RadioButton
            android:id="@+id/rb_mine"
            android:text="我的"
            android:drawableTop="@drawable/main_tab_mine_selector"
            style="@style/MainRadioButtonStyle" />

    </RadioGroup>

</LinearLayout>
